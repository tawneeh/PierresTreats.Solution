@{
  Layout = "_Layout";
}

@model PierresTreats.Models.Treat

<h3>Add a new Treat</h3>

@using (Html.BeginForm())
{
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)

    <input type="submit" value="Add new treat" class="btn btn-dark btn-sm" />
}